<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_menu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="mangoabliu.finalproject.MenuActivity"
    android:background="@drawable/bg_indexpage"
    >


    <mangoabliu.finalproject.Layout.FontTextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:paddingBottom="@dimen/menu_text_paddingBottom"
        android:paddingStart="@dimen/menu_text_paddingStart"
        android:id="@+id/tv_menu"
        android:text="@string/menu_hint"
        android:textColor="@color/clr_menu_text"
        android:textSize="@dimen/menu_textsize"
        android:shadowColor="@color/clr_map_shadow"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="3"/>

    <ImageButton
        android:id="@+id/btn_menu_setting"
        android:layout_width="@dimen/menu_btn_setting_size"
        android:layout_height="@dimen/menu_btn_setting_size"
        android:background="@drawable/btn_setting"
        android:layout_above="@+id/tv_menu"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="@dimen/menu_btn_marginEnd"
        android:layout_marginBottom="@dimen/menu_btn_marginBottom" />
</RelativeLayout>
